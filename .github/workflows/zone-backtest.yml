name: Backtest zone retest strategy

on:
  workflow_dispatch:
    inputs:
      outputsize:
        description: 'Antall historiske barer å hente per symbol/timeframe (gratisplan tåler vanligvis opptil 5000)'
        required: false
        default: '5000'
      symbol:
        description: 'Valgfritt: kjør kun ett symbol (f.eks. XAU/USD). Tom = alle 4.'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  zone-backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run zone retest backtest
        env:
          TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API_KEY }}
          ZONE_BACKTEST_OUTPUTSIZE: ${{ github.event.inputs.outputsize }}
          ZONE_BACKTEST_SYMBOL: ${{ github.event.inputs.symbol }}
        run: node src/zone/backtest.js

      - name: Commit backtest results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/zone-backtest-*.json
          git diff --staged --quiet || git commit -m "Zone retest backtest results [skip ci]"
          git push
